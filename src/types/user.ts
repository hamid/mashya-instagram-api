export interface birthdate {
    year: number
    month: number
    day: number
}
export enum gender {
    male = "male",
    female = "female"
}
export interface phoneNumber {
    countryCode: number
    phone: number
}